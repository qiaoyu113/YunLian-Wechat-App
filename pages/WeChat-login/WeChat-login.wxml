<view class="page">
    <view class="img">
        <image src="{{userInfo.avatarUrl}}"></image>
    </view>
    <view class="admin-name">{{userInfo.nickName}}</view>

    <view class="phone" bindtap="regiest">注册并绑定企智网账号</view>
    <view class="phone1" bindtap="bind">绑定企智网已有账号</view>
</view>